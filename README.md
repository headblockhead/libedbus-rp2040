# edbus-rp2040
A library that implements the EDBUS protocol using the Raspberry Pi RP2040's PIO.

## Tasks

### Build

Directory: build

```bash
cmake ..
cmake --build . -j $(nproc)
```

### Clean

```bash
rm -rf build
mkdir build
```
